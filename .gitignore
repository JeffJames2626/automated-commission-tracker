# Customer data — NEVER commit these
*.json
*.csv
*.xls
*.xlsx
alp-sales-backup*
alp-FULL-restore*

# …but these config files MUST be committed (they carry no customer data)
!package.json
!package-lock.json
!vercel.json
Clients*.csv
Leads*.csv
Payments*.csv
Balances*.csv
invoice_audit*

# OS / editor / tooling
.DS_Store
Thumbs.db
*.log
.claude/
.vscode/
.idea/

Open Tracker.bat
